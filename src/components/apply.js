import React from "react";
import "../appStyles/App.css";
import AuthIndex from "./AuthIndex";

function apply() {
  return (
    <div id="hide">
      <AuthIndex />
    </div>
  );
}

export default apply;
